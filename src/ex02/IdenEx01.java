package ex02;

/**
 * 변수 작성법
 * 1. 카멜(낙타)표기법 사용
 * 2. 숫자를 먼저 사용하지 않는다.
 * 3. 특수문자는 사용하지 않는다.
 */
public class IdenEx01 {
    public static void main(String[] args) {
        // int public = 1; // java의 '키워드'는 변수명으로 사용 불가능
        int user_money; // 절대 사용안함!!
        int userMoney; // 낙타 표기법 (이유 : 변수명은 카멜 표기법을 사용)
        // int abc#;
        int method;
        double abc$$; // 특수문자 사용하지 말자
        // int 1stMoney; // 숫자를 제일 먼저 들어가게 변수명 사용 불가능
    }
}
